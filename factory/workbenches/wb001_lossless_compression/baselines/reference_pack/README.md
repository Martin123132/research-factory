# WB-001 reference codec pack

This directory contains one audited adapter and fourteen frozen parameter
profiles. Each profile has its own submission contract, so its parameters form
part of the candidate artifact hash.

The pack is a qualification ladder, not a claim that these codecs are globally
optimal. Exact compressed bytes are portable only when the runtime image is
also pinned. Local timings remain advisory.
