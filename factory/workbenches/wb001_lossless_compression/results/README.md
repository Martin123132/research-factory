# Public commissioning evidence

This directory contains only the bounded public artifacts needed to verify the
WB-001 construction and worker entry path from a clean clone.

Tracked material is limited to:

- the frozen public reference pack and its per-codec results;
- the public synthetic qualification candidate and frontier comparison;
- one expired, signed demonstration job token and its bounded public
  attestation.

These are commissioning fixtures with synthetic inputs and `demo:` operators.
They carry zero scientific credit, cannot promote a claim and do not make
WB-001 live. The attestation exposes a verdict and evidence commitment, not the
sealed evidence or holdout input.

Private corpora, evaluator signing keys, active tokens, detailed holdout
measurements and local result directories remain ignored and must never be
committed.
